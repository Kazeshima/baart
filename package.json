{
  "name": "baart",
  "private": true,
  "version": "1.1.6",
  "type": "module",
  "scripts": {
    "prepare:assets": "node scripts/prepare-school-icons.mjs && node scripts/prepare-fonts.mjs",
    "dev": "npm run prepare:assets && vite",
    "build": "npm run prepare:assets && vite build",
    "test": "node --test",
    "preview": "vite preview",
    "tauri": "tauri",
    "video:preview": "npm run prepare:assets && vite --config video/vite.config.js --open /video.html",
    "video:studio": "npm run prepare:assets && remotion studio video/remotion/index.jsx",
    "video:render": "npm run prepare:assets && node video/render-cli.mjs",
    "video:profile": "npm run prepare:assets && node video/profile-render.mjs",
    "video:smoke": "npm run prepare:assets && node video/smoke-render.mjs",
    "video:compositions": "npm run prepare:assets && remotion compositions video/remotion/index.jsx",
    "video:compare": "npm run prepare:assets && node scripts/render-video-comparisons.mjs",
    "static:compare": "npm run prepare:assets && node scripts/generate-readme-examples.mjs --compare",
    "docs:examples": "npm run prepare:assets && node scripts/generate-readme-examples.mjs",
    "renderer:prepare": "npm run prepare:assets && node scripts/prepare-renderer-runtime.mjs",
    "renderer:smoke": "npm run renderer:prepare && node scripts/smoke-renderer-sidecar.mjs",
    "desktop:dev": "npm run renderer:prepare && npm run dev",
    "desktop:build": "npm run renderer:prepare && npm run build"
  },
  "dependencies": {
    "@dnd-kit/core": "6.3.1",
    "@dnd-kit/sortable": "10.0.0",
    "@dnd-kit/utilities": "3.2.2",
    "@remotion/player": "4.0.484",
    "@tauri-apps/api": "^2.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remotion": "4.0.484",
    "zod": "4.3.6",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@remotion/bundler": "4.0.484",
    "@remotion/cli": "4.0.484",
    "@remotion/renderer": "4.0.484",
    "@tauri-apps/cli": "^2.1.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.3.4"
  }
}
